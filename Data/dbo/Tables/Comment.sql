CREATE TABLE [dbo].[Comment]
(
    Id INT NOT NULL IDENTITY PRIMARY KEY,
    [PostId] INT NOT NULL, 
    [UserId] NVARCHAR(256) NOT NULL,
    [Text] NVARCHAR(MAX) NOT NULL, 
    [Date] DATETIME2(0) NULL DEFAULT SYSDATETIME()
   
)
